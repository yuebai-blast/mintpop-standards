# MintPop Keeper

> 示例产品页 —— 请按 Keeper 实际情况编辑。

密码 / 凭据管理桌面端（示例简介）。

## 遵循的品牌总规范

- **账号**：作为 OIDC 公开客户端（Public + PKCE）接入 → [统一账号接入](/global/account-integration)（`INVARIANT`）。
- **品牌**：命名 `MintPop Keeper`、Logo 用法 → [品牌规范](/global/brand)（`INVARIANT`）。
- **设计**：以[设计基线](/global/design-baseline)为起点，本产品的自定义见 [设计规范](/products/keeper/design)。

## 本产品素材

放在 `docs/public/assets/products/keeper/`，按 URL 直接引用：

| 用途 | 路径 |
|---|---|
| 产品 Logo | `/assets/products/keeper/logo.svg` |
| 应用图标 | `/assets/products/keeper/icon.svg` |

## 平台

桌面端目标平台：**Apple Silicon Mac + Windows**（不支持 Intel Mac）。
