# Keeper 设计规范

> 示例 —— 只写**本产品相对[设计基线](/global/design-baseline)的自定义**，其余继承基线。

## 主色覆盖

Keeper 用更冷静的蓝绿，覆盖基线薄荷绿：

| 语义 | 基线默认 | Keeper 覆盖 |
|---|---|---|
| 主色 Brand | `#3DDC97` | `#2E9E8F`（示例） |

## 其余

- 字体、间距、圆角：**沿用基线**，未覆盖。
- 可访问性：遵循基线（WCAG AA）。
